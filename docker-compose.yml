services:
  pm-mvp:
    build: .
    container_name: pm-mvp
    ports:
      - "80:8000"
    env_file:
      - .env
    environment:
      - KANBAN_DB_PATH=/data/kanban.db
    volumes:
      - pm-data:/data
    restart: unless-stopped

volumes:
  pm-data:
